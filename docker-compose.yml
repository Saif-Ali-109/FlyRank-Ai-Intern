services:
  app:
    build: .
    container_name: task-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - task-data:/data
    restart: unless-stopped

volumes:
  task-data:
