[project]
name = "week-2"
version = "3.0.0"
description = "Task API with SQLite persistence + Docker"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.140.0",
    "uvicorn[standard]>=0.51.0",
    "python-dotenv>=1.0.0",
]
